{
  "name": "opencode-plugin-continual-learning",
  "version": "1.0.0",
  "description": "OpenCode plugin for continual learning workflow - Learn from every task, build persistent knowledge",
  "main": "./dist/index.js",
  "type": "module",
  "peerDependencies": {
    "@opencode-ai/plugin": "^1.0.126"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.0.126",
    "@types/node": "^20.0.0"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  }
}
